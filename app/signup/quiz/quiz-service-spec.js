describe('quizService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(quizService) {

	//expect(quizService.doSomething()).toEqual('something');

  }));

});