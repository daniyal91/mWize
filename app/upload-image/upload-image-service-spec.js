describe('uploadImageService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(uploadImageService) {

	//expect(uploadImageService.doSomething()).toEqual('something');

  }));

});