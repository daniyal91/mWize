describe('ticketService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(ticketService) {

	//expect(ticketService.doSomething()).toEqual('something');

  }));

});