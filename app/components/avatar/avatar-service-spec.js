describe('avatarService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(avatarService) {

	//expect(avatarService.doSomething()).toEqual('something');

  }));

});