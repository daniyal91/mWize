describe('phoneModelService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(phoneModelService) {

	//expect(phoneModelService.doSomething()).toEqual('something');

  }));

});