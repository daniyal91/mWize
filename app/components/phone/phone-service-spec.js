describe('phoneService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(phoneService) {

	//expect(phoneService.doSomething()).toEqual('something');

  }));

});