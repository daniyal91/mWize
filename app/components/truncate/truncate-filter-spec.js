describe('truncateFilter', function() {

	beforeEach(module('app'));

	it('should ...', inject(function($filter) {

        var filter = $filter('truncateFilter');

		expect(filter('input')).toEqual('output');

	}));

});