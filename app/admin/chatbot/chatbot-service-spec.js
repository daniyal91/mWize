describe('chatbotService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(chatbotService) {

	//expect(chatbotService.doSomething()).toEqual('something');

  }));

});